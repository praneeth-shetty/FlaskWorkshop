# FlaskWorkshop
Creating a simple app using flask, bootstrap and some public API's.

To run this application
Download the code > extract it to any desired location > open the folder in VSCODE > open cmd inside VSCODE (ctrl and ~)
The run the following command : python app.py
After running the command, go to this url in your browser : localhost:5000

the app should be up and running.
